{
  "name": "@excelera/config",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@3.0.2",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "npx rimraf dist && rm *.tsbuildinfo",
    "compile": "tsc --build"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT"
}
